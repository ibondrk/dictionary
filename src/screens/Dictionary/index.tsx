import React from "react";
import { Text, View, Button } from "react-native";

import { gStyle } from "../../../styles/style";

export const Dictionary = ({ navigation }) => {
  return (
    <View style={gStyle.main}>
      <Text>Dictionary connected, and will be here</Text>
    </View>
  );
};
